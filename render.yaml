services:
  - type: web
    name: chantrea-cv
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ADMIN_PASSWORD
        sync: false
      - key: NODE_VERSION
        value: "20"
